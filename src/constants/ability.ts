export enum Role {
  ADMIN = 'admin',
  USER = 'user',
  ALL = 'all',
}

export enum AbilityAction {
  Manage = 'manage',
  Create = 'create',
  Read = 'read',
  Update = 'update',
  Delete = 'delete',
}
